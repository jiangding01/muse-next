# Legacy compatibility notice

Muse Next is intended as an independent reimplementation based on observed file formats, documented behavior and clean-room analysis.

The repository scaffold intentionally contains no legacy executable, bundled song, proprietary font, icon or binary resource. Keep that boundary when adding compatibility fixtures: prefer synthetic files that exercise the same syntax without copying protected content.
